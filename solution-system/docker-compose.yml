services:
  adk-agent:
    build:
      context: ./adg-agents
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
    ports:
      - "8081:8081"

  mcp-server:
    build:
      context: ./mcp-server
    ports:
      - "8000:8000"
